//
//  ContentView.swift
//  dictionary
//
//  Created by IZ on 11.04.2024.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Widget mode supports only")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
